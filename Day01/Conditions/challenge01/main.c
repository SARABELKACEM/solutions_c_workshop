#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
int main() {
int n;
printf("entrer un nombre entier:");
scanf("%d",&n);
if (n % 2 == 0){
printf("le nombre %d est paire",&n);
}
else{ ("le nombre %d est impaire",&n);
}
return 0;
}
